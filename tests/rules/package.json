{
  "name": "smart-dustbin-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore Security Rules tests for Smart Dustbin, run against the local emulator.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-sdbs \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.30.2"
  }
}
